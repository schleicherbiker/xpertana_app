
function addSession() {
    session = $("<div>");
    session.addClass("session");
    session.text("whats up");
    $("#sessionList").append(session);
}

addSession();
addSession();

$("#sessionChat").text("blahsdgfds");